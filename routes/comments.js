const express = require('express');
const router = express.Router({ mergeParams: true });

router.get('/', () => {});
router.post('/', () => {});
router.patch('/', () => {});
router.delete('/', () => {});

module.exports = router;
